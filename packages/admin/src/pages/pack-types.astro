---
import Page from '@admin/layouts/Page.astro';
import { getAllPackTypes } from '@core/lib/pack-type';
import { routes } from '@admin/constants';

import { PageHeader, PageTitle, Anchor } from '@admin/components/text';

import PackTypeTable from '@admin/components/pack-type/PackTypeTable';

const packTypes = await getAllPackTypes();
---

<Page title="Pack Types">
	<PageHeader>
		<PageTitle>Pack Types</PageTitle>
		<Anchor href={routes.ADMIN.CREATE.PACK_TYPE}>Add Pack Type</Anchor>
	</PageHeader>
	<div class="mx-auto max-w-2xl">
		<PackTypeTable {packTypes} client:load />
	</div>
</Page>
