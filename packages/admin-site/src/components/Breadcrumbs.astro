---
export interface Path {
	label: string
	href?: string | undefined
}

interface Props {
	path: Path[]
}
---

<section class="flex gap-2 p-4 font-medium underline-offset-4">
	{
		Astro.props.path.map(({ label, href }, index) =>
			index === Astro.props.path.length - 1 ? (
				<p class="block font-heading font-bold">{label}</p>
			) : href ? (
				<>
					<a rel="prefetch" class="underline font-heading" href={href}>
						{label}
					</a>
					<p class="block font-heading">/</p>
				</>
			) : (
				<>
					<p class="block font-heading">{label}</p>
					<p class="block font-heading">/</p>
				</>
			)
		)
	}
</section>
