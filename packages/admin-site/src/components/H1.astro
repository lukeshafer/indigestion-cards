---
import type { HTMLAttributes } from 'astro/types'
type Props = HTMLAttributes<'h1'>
const { props } = Astro

const className = props.class
delete props.class
---

<h1 {...props} class={`${className} text-center text-4xl uppercase font-heading font-bold my-2`}>
	<slot />
</h1>
