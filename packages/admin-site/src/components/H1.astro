---
import type { HTMLAttributes } from 'astro/types'
type Props = HTMLAttributes<'h1'>
const { props } = Astro

const className = props.class
delete props.class
---

<h1 {...props} class={`${className} page-heading`}>
	<slot />
</h1>
