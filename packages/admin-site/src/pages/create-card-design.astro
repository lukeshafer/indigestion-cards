---
import Base from '../layouts/Base.astro'
import { getAllSeries } from '@lil-indigestion-cards/core/card'
import CreateCardForm from '../components/CreateCardForm'

/*const series = await getAllSeries()*/

// TODO: Protect the API route
---

<Base title="Add Card Design">
	<h1>Add Card Design</h1>
	<p>Use the form below to add a new card design.</p>
	<CreateCardForm
		series={[{ seriesId: 'test', seriesName: 'test' }]}
		client:load
	/>
</Base>
