---
import Page from '@/layouts/Page.astro'
import { useUser } from '@/session'

const user = useUser(Astro.cookies)

console.log(user)
---

<Page title="index" session={user}>
	<h1 class="text-3xl font-heading font-bold uppercase text-brand-secondary">lil indigestion</h1>
	{
		user ? (
			<a href="/auth/logout" rel="noreferrer">
				Logout
			</a>
		) : (
			<a href="/auth/login" rel="noreferrer">
				Login
			</a>
		)
	}
</Page>
