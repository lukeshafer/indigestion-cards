---
import TiltCardEffect from './cards/TiltCardEffect';

export type CardProps = Props;
interface Props {
	rarityName: string;
	frameUrl: string;
	imgUrl: string;
	cardName: string;
	cardDescription: string;
	designId: string;
	cardNumber: number;
	totalOfType: number;
	scale?: number;
}

const {
	frameUrl,
	imgUrl,
	cardName,
	cardDescription,
	cardNumber,
	totalOfType,
	scale = 1,
} = Astro.props;
---

<TiltCardEffect client:load shiny={true}>
	<article
		class="card-wrapper card-aspect-ratio relative w-72 bg-cover text-left"
		style={{ 'background-image': `url(${frameUrl})`, scale }}>
		<img
			src={imgUrl}
			alt={cardName}
			class="design-img absolute left-[9%] top-[15%] h-[51%] w-[82%] object-cover"
		/>
		<h3
			style={{
				'--left': '12%',
				'--top': '4.4%',
				width: 'calc(100% - var(--left) * 2)',
				left: 'var(--left)',
				top: 'var(--top)',
			}}
			class="font-display absolute text-lg font-bold italic text-slate-900">
			{cardName}
		</h3>
		<p
			style={{
				'--left': '10%',
				top: '70%',
				height: '15%',
				width: 'calc(100% - var(--left) * 2)',
				left: 'var(--left)',
			}}
			class="card-description absolute p-2 font-sans font-normal text-black">
			{cardDescription}
		</p>
		<p class="font-display absolute bottom-1 right-3 text-xs font-bold italic text-white">
			{cardNumber} / {totalOfType}
		</p>
	</article>
</TiltCardEffect>
