---
interface Props {
	id?: string;
	heading?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
}

const { id, heading = 'h1', ...rest } = Astro.props;

const Element = heading;
---

<Element id={id} class="font-heading my-2 text-3xl font-bold uppercase" {...rest}>
	<slot />
</Element>
