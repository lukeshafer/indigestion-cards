---
interface Props {
	id?: string;
	class?: string;
}

const { id, class: className = '', ...rest } = Astro.props;
---

<tr
	class="even:bg-brand-100 group odd:bg-gray-300"
	{id}
	{...rest}
	class:list={{ [className]: true }}>
	<slot />
</tr>
